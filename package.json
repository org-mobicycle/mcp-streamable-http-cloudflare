{
  "name": "mcp-streamable-http-mobicycle-ou",
  "version": "1.0.0",
  "description": "Cloudflare MCP Server with Streamable HTTP transport — KV operations + email pipeline tools",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "agents": "^0.0.74",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "wrangler": "^4.0.0",
    "typescript": "^5.7.0"
  }
}
